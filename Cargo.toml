[package]
name = "orx-soa"
version = "0.1.0"
edition = "2024"
publish = ["artifactory"]

[dependencies]
orx-parallel = { git = "https://github.com/orxfun/orx-parallel", branch = "collect-into-soa" }
orx-priority-queue = { version = "1.9.0", default-features = false }
